# TODO: This is a skeleton deployment workflow. Candidates should
# implement a full CI/CD pipeline with the following stages:
#
# 1. Build and push container image to a registry (ECR, GHCR, etc.)
# 2. Deploy to dev environment (automatic on merge to main)
# 3. Run integration tests against dev
# 4. Promote to staging (automatic if dev tests pass)
# 5. Promote to production (manual approval gate)
#
# Considerations:
# - Use OIDC federation for AWS authentication (no static credentials)
# - Tag images with git SHA for immutable, traceable deployments
# - Implement proper concurrency controls for each environment
# - Add deployment status notifications (Slack, GitHub, etc.)
# - Consider using reusable workflows for DRY pipeline definitions

name: Deploy

on:
  workflow_run:
    workflows: ["CI"]
    branches: [main]
    types: [completed]

# TODO: Add concurrency groups per environment
# concurrency:
#   group: deploy-${{ inputs.environment }}
#   cancel-in-progress: false

jobs:
  # TODO: Implement image build and push
  # build-and-push:
  #   runs-on: ubuntu-latest
  #   if: ${{ github.event.workflow_run.conclusion == 'success' }}
  #   permissions:
  #     id-token: write  # Required for OIDC
  #     contents: read
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Configure AWS credentials (OIDC)
  #       uses: aws-actions/configure-aws-credentials@v4
  #       with:
  #         role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
  #         aws-region: us-east-1
  #     - name: Login to Amazon ECR
  #       uses: aws-actions/amazon-ecr-login@v2
  #     - name: Build and push
  #       run: |
  #         docker build -t $ECR_REPO:${{ github.sha }} .
  #         docker push $ECR_REPO:${{ github.sha }}

  # TODO: Implement deploy-dev job
  # deploy-dev:
  #   needs: build-and-push
  #   environment: dev
  #   steps:
  #     - Update image tag in gitops repo/directory
  #     - Wait for ArgoCD sync
  #     - Run smoke tests

  # TODO: Implement deploy-staging job
  # deploy-staging:
  #   needs: deploy-dev
  #   environment: staging
  #   steps:
  #     - Promote image tag to staging
  #     - Run integration tests
  #     - Soak test period

  # TODO: Implement deploy-prod job (with manual approval)
  # deploy-prod:
  #   needs: deploy-staging
  #   environment:
  #     name: production
  #     # Manual approval required
  #   steps:
  #     - Promote image tag to production
  #     - Monitor canary analysis
  #     - Verify rollout completion
  placeholder:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - run: echo "TODO - Implement deployment pipeline"
