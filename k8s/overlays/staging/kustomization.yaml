apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Staging mirrors production replica count for realistic testing.
replicas:
  - name: opsboard
    count: 2

commonLabels:
  env: staging

# TODO: Add staging-specific configuration.
# Consider:
#   - Pinning a specific image tag (not :latest) for reproducible deployments
#   - Staging database connection string via configmap or secret patch
#   - Enabling additional logging or monitoring
#
# images:
#   - name: opsboard
#     newTag: staging-abc1234
#
# patches:
#   - target:
#       kind: ConfigMap
#       name: opsboard-config
#     patch: |-
#       ...
