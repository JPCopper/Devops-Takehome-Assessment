apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Override replica count for dev: run a single instance to save resources.
replicas:
  - name: opsboard
    count: 1

commonLabels:
  env: dev

# TODO: Add dev-specific patches here.
# Examples of things you might configure for dev:
#   - Lower resource requests/limits
#   - Debug-level logging via configmap patch
#   - Relaxed security policies for local development
#   - Image tag override (e.g., opsboard:dev-latest)
#
# patches:
#   - target:
#       kind: Deployment
#       name: opsboard
#     patch: |-
#       ...
